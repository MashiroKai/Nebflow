# 客户端 Logo v4 替换 — QA 验收报告（PASS 放行）

- **日期**：2026-09-01
- **验收人**：qa-frontend
- **产出者**：Frontend
- **交付**：分支 `feat/logo-v4`，commit **51f438c7**，仓库 `/Users/dev/Claude code/Nebflow`
- **验收基座**：**隔离真实 http4s 实例** `http://localhost:8094`（worktree `/tmp/nb-logo-v4` @ 51f438c7，java PID **75122**，health 200）
- **宿主保护**：8080 LISTEN = PID 66194 = 我所在宿主，全程未触碰；8094 已 PID 验身为独立 java 实例（cwd=`/private/tmp/nb-logo-v4`）
- **结论**：**PASS —— 放行。**

---

## 验收条件逐项核对

### ① 亮色主题：头像 = 黑色 n + 绿点缀 #4caf50 ✅
- `imgSrc = css/logo-bright-4.png`，`sourceSrcset = css/logo-dark-4.png`（亮/暗分支正确指向 -4）
- 头像 canvas 抽色 = `76,175,80`（绿）+ `0,0,0`（黑）→ **黑色 n + 绿点缀**，即 `#4caf50` ✓
- 未登录态 `opacity .45 + grayscale .7` 变灰为**预期**（交付已声明，非资产问题）——位图本身是纯黑+纯绿，灰色是 CSS filter，不是抗锯齿污染

### ② 暗色主题：头像 = 白色 n + 绿点缀 ✅
- 暗色 theme 头像抽色 = `76,175,80`（绿）+ `255,255,255`（白）→ **白色 n + 绿点缀** ✓
- NebLink 未登录占位图：`src/css/logo-bright-4.png`、`srcset/css/logo-dark-4.png` 均命中 -4，img 加载 224×224，抽色 `76,175,80`+`0,0,0`（亮色占位），`opacity:0.7` 符合既有规则 ✓

### ③ favicon 为新 logo、像素锐利无抗锯齿 ✅
像素级断言（PIL）：

| 文件 | 尺寸 | 唯一色数 | black | green | 中间色(抗锯齿) |
|---|---|---|---|---|---|
| favicon-32.png | 32×32 | **3** | (0,0,0,255)×384 | (76,175,80,255)×64 | **0** |
| favicon-16.png | 16×16 | **3** | (0,0,0,255)×96 | (76,175,80,255)×16 | **0** |

- **恰好纯三色**：透明 / 黑 / `(76,175,80)`，**零中间色** → 锐利无抗锯齿 ✓
- 16px = 4px 整块、32px = 8px 整块（8 格网格，符合交付"4px/8px 整块"）
- 绿色 blob 精确 = `(76,175,80)` = `#4caf50`

### ④ 旧 lime 绿 #a8e61d 不再出现在任何 v4 引用点 ✅
```bash
grep -rn "a8e61d\|#a8e61d\|236,230,29" src/main/resources/web/  → NO lime green anywhere
grep -rn "logo-bright.png\|logo-dark.png" src/main/resources/web/ → NO bare v2 logo refs
```
- 无 lime 绿残留；无裸 v2 logo 引用；-4 引用点均在 index.html:63-64 / neblink.js:170-171

### ⑤ 静态资源可达性：均 200 + 正确 MIME ✅
真实实例逐条实测（含 `?v=4` query 与无 query 兜底）：

| 资源 | 状态码 | content-type | 大小 |
|---|---|---|---|
| css/logo-bright-4.png | **200** | image/png | 627B |
| css/logo-dark-4.png | **200** | image/png | 634B |
| favicon-32.png?v=4 | **200** | image/png | 348B |
| favicon-16.png?v=4 | **200** | image/png | 344B |
| favicon.ico?v=4 | **200** | image/x-icon | 15086B |
| favicon-32.png（无 query 兜底） | 200 | image/png | 348B |
| favicon.ico（无 query 兜底） | 200 | image/x-icon | 15086B |
| css/logo-bright.png（旧 v2 兜底保留） | 200 | - | 598B |

- **`?v=4` query 被 http4s 静态路由正确匹到**（http4s 只匹 path 不含 query；无 query 也 200 兜底）✓
- 旧 v2 文件仍在（200）——交付所述"已缓存页面兜底，下 release 删" ✓

---

## 附加独立核验
- **改动范围**：`git show 51f438c7 --stat` = 8 文件，全在 `src/main/resources/web/`，**零 Scala** ✓
- **404/403 甄别**：控制台 403 全为未认证 `/api/*`（canvas-tabs / neblink/status / nf-file）+ `/ws` 握手——需登录 token，属正常未登录行为，**与 logo 无关**；logo/favicon 资源**零 4xx/5xx**、零 request-failed ✓
- **渲染盒尺寸**：`.activity-avatar-logo` = **24×24**（nav.css 22→24 生效）✓；`.neblink-logged-out-logo` = **32×32**（36×30→32×32 生效，对齐 8 格网格）✓
- 控制台 logo/favicon 相关错误：**无** ✓

---

## 结论
**PASS —— 放行。** 五项验收条件全绿：亮/暗主题 n + 绿点缀（#4caf50，正确互换黑白/白黑）、favicon 纯三色锐利无抗锯齿、无 lime 绿残留、`?v=4` 静态资源全部 200 + MIME 正确、改动零 Scala 且范围最小。
- 隔离实例确认在跑新代码（served logo-bright-4.png 627B / logo-dark-4.png 634B，favicon 348B/15086B 与文件一致，非快照陈旧）
- 随下次重启包生效（宿主 8080 未重启，当前仍是旧 logo——符合交付与管理要求）

安全：8094 = java PID 75122（cwd=/private/tmp/nb-logo-v4），8080 = 66194 宿主全程未触碰。

报告：~/.nebflow/docs/Nebflow/20260901_logo-v4-qa.md

注意：本次起隔离实例在 /tmp/nb-logo-v4（detached worktree @ 51f438c7）——veiwtree 待 Manager 决策是否清理。
