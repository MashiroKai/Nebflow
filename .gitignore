# Build
target/
project/target/
project/project/

# IDE
.idea/
.vscode/
.metals/
.bloop/
.bsp/

# System
.DS_Store
Thumbs.db

# Nebflow runtime（.nebflow/ 整体忽略，唯 Spec/ 为 skills→plugins 映射标准源，可跟踪）
# dispatcher-ctx 批照 memory-plan 配方：runtime 全忽略，仅 Spec/ 入 track（spec 随分支走）
.nebflow/*
!.nebflow/Spec/
.nebflow-qc.log

# Internal / development-only
.claude/
.codex/
.envrc
.java-version
SKEL
archives/
temp/
tmp/
docs/
test-envs.sh
CHANGELOG.md
*demo*.html
/nebflow
nul
cloud/**/node_modules/
cloud/node_modules/

# Sensitive
*.env
*.secret
credentials.json
dist/
beta.md
# Root-level scratch file only (51c0882d). Must NOT match the vendored monaco
# TS language dirs (vendor/monaco/vs/{language,basic-languages}/typescript/).
/typescript

# ZCode agent workspace
.zcode/

# Node / Playwright
node_modules/
test-results/
# NOTE: package.json / package-lock.json are TRACKED since P1 (esbuild build
# tooling, scripts/build-web.mjs) - they used to be ignored as scratch files.
playwright.config.mjs

# Test artifacts
gui-test-screenshots/
swarm/

# Temp review reports
.code-review-report.html
.nebflow-review.html

# Internal handover doc (contains server credentials)
NEBLINK_HANDOVER.md
build/

# P1 web build tooling
node_modules/

# sbt 沙箱重定向目录（工作树内测试运行产物）
.sbt/
.ivy2/
.coursier/

# upload-deps.sh staging (batch artifacts, never committed)
deps-staging/

# Python bytecode cache (packaging/gen-icons.py side artifacts)
__pycache__/
