# Build
target/
project/target/
project/project/

# IDE
.idea/
.vscode/
.metals/
.bloop/
.bsp/

# System
.DS_Store
Thumbs.db

# Nebflow runtime（Spec 例外：契约对齐文档入库）
# 注：.nebflow/ → .nebflow/* + !.nebflow/Spec/ 使 .nebflow/Spec/ 可跟踪。
# ⚠️ 此 hunk 与 memory-plan / merge-node / dispatcher-ctx 三批同文件改动交叠，
# 宿主按各自基于 main 的配方重放（friend-contract-align 批次注明）。
.nebflow/*
!.nebflow/Spec/
.nebflow-qc.log

# Internal / development-only
.claude/
.codex/
.envrc
.java-version
SKEL
archives/
temp/
tmp/
docs/
test-envs.sh
CHANGELOG.md
*demo*.html
/nebflow
nul
cloud/**/node_modules/
cloud/node_modules/

# Sensitive
*.env
*.secret
credentials.json
dist/
beta.md
# Root-level scratch file only (51c0882d). Must NOT match the vendored monaco
# TS language dirs (vendor/monaco/vs/{language,basic-languages}/typescript/).
/typescript

# ZCode agent workspace
.zcode/

# Node / Playwright
node_modules/
test-results/
# NOTE: package.json / package-lock.json are TRACKED since P1 (esbuild build
# tooling, scripts/build-web.mjs) - they used to be ignored as scratch files.
playwright.config.mjs

# Test artifacts
gui-test-screenshots/
swarm/

# Temp review reports
.code-review-report.html
.nebflow-review.html

# Internal handover doc (contains server credentials)
NEBLINK_HANDOVER.md
build/

# P1 web build tooling
node_modules/
