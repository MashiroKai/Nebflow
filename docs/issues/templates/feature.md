# Feature Template

> **[必填]** 标记为必须填写，其余按需填写。
> 术语定义见 [`_glossary.md`](./_glossary.md)。
> **必填时机说明**：`[创建]` = 创建时必须填写；`[结束]` = 关闭前必须填写；未标注 = 按需填写
> 状态流转见 `_glossary.md` → 对应类型。
>
> **升级规则**：见 `_glossary.md` → Quick Fix 升级规则。涉及行为变更时，关闭 quickfix，改用本模板。

---

## [创建] 元信息

| 字段 | 内容 |
|------|------|
| 负责人 | @username |
| 状态 | 见 `_glossary.md` → 状态流转 |
| 标签 | 见 `_glossary.md` → 标签 |
| 优先级 | P0-阻塞 / P1-高 / P2-中 / P3-低 |
| 创建日期 | 2026-05-06 |
| 目标日期 | YYYY-MM-DD / 未确定 |
| 预估工时 | Xh / 未评估 |
| 实际工时 | Xh / 进行中 |

---

## [创建] 一句话描述

> 用一句话精确定义本 issue。例见各类型模板。
> 例：支持多模型切换，允许用户在对话中动态切换底层 LLM。

---

## 背景（可选）

> 按需填写。如果本功能依赖前置工作，简要说明上下文。

> 例：Issue `003-extract-model-client.md` 已完成模型客户端抽象，本 issue 在此基础上实现运行时切换逻辑。

---

## [创建] 动机

**为什么需要这个功能？**

- 描述用户场景或业务需求。
- 如果不做，当前有什么 workaround？workaround 的问题是什么？

---

## [创建] 目标

**交付物是什么？**

> 例：用户可在对话中通过 UI 或 API 切换模型，切换后新请求路由到目标模型，历史消息保留。

---

## 优先级（可选）

> 按需填写。功能需求建议填写目标版本和发布日期。

| 属性 | 评估 |
|------|------|
| 优先级 | P0-阻塞 / P1-高 / P2-中 / P3-低 |
| 目标版本 | vX.Y.Z / 未确定 |
| 目标发布日期 | YYYY-MM-DD / 无 |

---

## [创建] 范围

### In Scope

- [ ] 明确列出必须包含的内容

### Out of Scope

- [ ] 明确列出不包含的内容
- [ ] 本次不处理的关联问题（如有，拆分为新 issue）：

> 例：本次不实现模型性能对比图表，仅提供切换能力。

---

## [创建] 需求

### 功能需求

- [ ] 功能点 1
- [ ] 功能点 2

### 非功能需求

- [ ] 性能要求
- [ ] 兼容性要求（向后兼容 / 破坏性变更）

---

## 设计

### 接口/行为变更

- 新增接口：
- 变更接口：
- 废弃接口：

### 关键决策

| 决策点 | 选项 A | 选项 B | 选择 | 理由 |
|--------|--------|--------|------|------|
| ... | ... | ... | ... | ... |

### 待决策项

- [ ] 问题 1：...
- [ ] 问题 2：...

### 代码示例

```scala
// 使用示例或接口定义
```

---

## [创建] 兼容性

- **向后兼容：** Yes / No
- **迁移指南：** 如果需要用户/调用方调整，说明步骤
- **废弃计划：** 如有旧接口废弃，说明时间表

---

## [结束] 成功标准

**如何量化地判断是否成功？**

- [ ] 可量化的成功指标 1（例：切换模型延迟 < 100ms）
- [ ] 可量化的成功指标 2（例：单元测试覆盖率 > 80%）
- [ ] 所有功能需求已满足

---

## 风险与缓解

| 风险 | 严重程度 | 缓解措施 |
|------|----------|----------|
| | Low / Medium / High | |

---

## [结束] 验证

### 功能验证

- [ ] 步骤一：...
- [ ] 步骤二：...

### 测试策略

- [ ] 单元测试覆盖核心逻辑
- [ ] 集成测试覆盖端到端场景

### 回归检查

- [ ] 现有功能不受影响

---

## 关联

- Depends on `xxx.md` — 前置依赖
- Blocks `yyy.md` — 阻塞后续工作
- Related to `zzz.md` — 相关但无依赖
- Supersedes `www.md` — 替代旧 issue
- Introduced by `commit/PR` — 引入该问题的变更

---

## 变更文件

| File | Action | Notes |
|------|--------|-------|
| | | |

> 创建时填写"计划"，关闭前更新为"实际"。

| `Foo.scala` | **Modify** | 新增接口 |
| `Bar.scala` | **Create** | 核心实现 |

---

## [结束] 关闭原因

> 见 `_glossary.md` → 关闭原因。必须选择一项。

- [ ] 已发布
- [ ] 重复 — 重复 issue：
- [ ] 不予处理 — 理由：
- [ ] 已过时
- [ ] 已取消 — 理由：

---

## [结束] 复盘（可选）

> `[结束]` 时填写。若关闭原因为"重复"/"无法复现"/"已过时"，可跳过。

| 问题 | 回答 |
|------|------|
| 目标达成了吗？ | |
| 有什么意外？ | |
| 用户反馈如何？ | |
| 工时预估偏差原因？ | |
