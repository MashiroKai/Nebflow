# Test Template

> **[必填]** 标记为必须填写，其余按需填写。
> 术语定义见 [`_glossary.md`](./_glossary.md)。
> **必填时机说明**：`[创建]` = 创建时必须填写；`[结束]` = 完成前必须填写；未标注 = 按需填写
> 状态流转见 `_glossary.md` → 对应类型。

---

## [创建] 元信息

| 字段 | 内容 |
|------|------|
| 负责人 | @username |
| 状态 | 见 `_glossary.md` → 状态流转 |
| 标签 | `test` + 见 `_glossary.md` → 标签（必须附加 `test` 前缀） |
| 优先级 | P0-阻塞 / P1-高 / P2-中 / P3-低 |
| 创建日期 | YYYY-MM-DD |
| 目标日期 | YYYY-MM-DD / 未确定 |
| 预估工时 | Xh / 未评估 |
| 实际工时 | Xh / 进行中 |

---

## [创建] 一句话描述

> 用一句话精确定义本 issue。
> 例：为 SessionActor 补充单元测试，覆盖消息路由和状态转换逻辑。

---

## [创建] 动机

**为什么要补充/改进测试？**

- 当前测试覆盖的缺口是什么？
- 不补充测试的风险是什么？（如：回归 bug、重构阻力、CI 无法捕获问题）

---

## [创建] 目标

**测试完成后的成功状态是什么？**

> 例：SessionActor 核心消息处理逻辑单元测试覆盖率 > 80%，CI 中运行时间 < 30s。

---

## [创建] 范围

### In Scope

- [ ] 明确列出必须包含的测试内容

### Out of Scope

- [ ] 明确列出不包含的测试内容
- [ ] 本次不处理的关联问题（如有，拆分为新 issue）：

---

## [创建] 测试策略

### 测试类型

- [ ] 单元测试 — 覆盖范围：
- [ ] 集成测试 — 覆盖范围：
- [ ] 端到端测试 — 覆盖范围：
- [ ] 性能测试 — 覆盖范围：
- [ ] 其他：

### 测试框架/工具

| 层级 | 框架/工具 | 版本 |
|------|-----------|------|
| 单元测试 | | |
| 集成测试 | | |
| Mock/Stub | | |

### 关键测试场景

| 场景 | 输入 | 期望输出 | 测试类型 |
|------|------|----------|----------|
| | | | |

---

## [创建] 测试数据

> 如需特殊测试数据（fixture、mock 数据、数据库状态），在此说明。

---

## 风险与缓解

| 风险 | 严重程度 | 缓解措施 |
|------|----------|----------|
| | Low / Medium / High | |

---

## [结束] 验证

### 覆盖率检查

```bash
# 运行测试并生成覆盖率报告
sbt coverage test coverageReport
```

- [ ] 目标覆盖率已达成
- [ ] 新增代码覆盖率 > X%

### 测试质量检查

- [ ] 所有测试用例可独立运行
- [ ] 无 flaky test（不稳定测试）
- [ ] 测试命名清晰，意图明确
- [ ] 测试执行时间合理

### CI 验证

- [ ] 本地 `sbt test` 通过
- [ ] CI pipeline 通过

---

## 关联

- Depends on `xxx.md` — 前置依赖
- Blocks `yyy.md` — 阻塞后续工作
- Related to `zzz.md` — 相关但无依赖
- Supersedes `www.md` — 替代旧 issue
- Introduced by `commit/PR` — 引入该问题的变更

---

## 变更文件

| File | Action | Notes |
|------|--------|-------|
| | | |

> 创建时填写"计划"，关闭前更新为"实际"。

| `FooSpec.scala` | **Create** | 新增单元测试 |
| `BarIntegrationSpec.scala` | **Create** | 新增集成测试 |

---

## [结束] 关闭原因

> 见 `_glossary.md` → 关闭原因。必须选择一项。

- [ ] 已完成
- [ ] 重复 — 重复 issue：
- [ ] 不予处理 — 理由：
- [ ] 已过时
- [ ] 已取消 — 理由：

---

## [结束] 复盘（可选）

> `[结束]` 时填写。若关闭原因为"重复"/"无法复现"/"已过时"，可跳过。

| 问题 | 回答 |
|------|------|
| 目标达成了吗？ | |
| 测试发现了多少潜在问题？ | |
| 测试维护成本如何？ | |
| 工时预估偏差原因？ | |
