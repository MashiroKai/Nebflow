# Bug Template

> **[必填]** 标记为必须填写，其余按需填写。
> 术语定义见 [`_glossary.md`](./_glossary.md)。
> **必填时机说明**：`[创建]` = 创建时必须填写；`[结束]` = 关闭前必须填写；未标注 = 按需填写
> 状态流转见 `_glossary.md` → 对应类型。
>
> **升级规则**：见 `_glossary.md` → Quick Fix 升级规则。满足任一条件时，关闭 quickfix，改用本模板。

---

## [创建] 元信息

| 字段 | 内容 |
|------|------|
| 负责人 | @username |
| 状态 | 见 `_glossary.md` → 状态流转 |
| 标签 | 见 `_glossary.md` → 标签 |
| 优先级 | P0-阻塞 / P1-高 / P2-中 / P3-低 |
| 创建日期 | 2026-05-06 |
| 目标日期 | YYYY-MM-DD / 未确定 |
| 预估工时 | Xh / 未评估 |
| 实际工时 | Xh / 进行中 |

---

## [创建] 一句话描述

> 用一句话精确定义本 issue。例见各类型模板。
> 例：WebSocket 连接断开时 SessionActor 未清理，导致内存泄漏。

---

## [创建] 影响评估

| 维度 | 评估 |
|------|------|
| 影响范围 | 全部用户 / 部分用户 / 仅开发者 |
| 严重程度 | Critical / High / Medium / Low |
| 数据风险 | 无 / 可能丢失 / 已确认丢失 |
| 复现频率 | 必现 / 高频 / 偶发 / 仅报告一次 |

---

## [创建] 复现

### 环境

- 版本/分支：
- 引入该 bug 的 commit/PR（如已知）：
- 运行方式：

### 步骤

1. 步骤一
2. 步骤二
3. 步骤三

### 实际行为

> 描述观察到的错误现象。

### 期望行为

> 描述正确的行为应该是什么。

---

## [结束] 根因分析

> 根据当前状态填写对应区块，未涉及的区块保留标题但留空。

### 根本原因（已定位时填写）

- 简要说明根本原因。
- 引用相关代码片段。

```scala
// 问题代码示例
```

### 排查记录（排查中/待排查时填写）

- 当前排查方向：
- 临时缓解措施（如有）：
- 可疑代码区域：

---

## 修复方案

- 简述修复思路。

```scala
// 修复后的代码示例
```

---

## [创建] 范围

### In Scope

- [ ] 修复该 bug 本身
- [ ] 补充回归测试

### Out of Scope

- [ ] 不引入新功能
- [ ] 不修改无关代码
- [ ] 本次不修复的关联问题（如有，拆分为新 issue）：

---

## 风险与缓解

| 风险 | 严重程度 | 缓解措施 |
|------|----------|----------|
| | Low / Medium / High | |

---

## [结束] 验证

### 复现验证

- [ ] 按复现步骤操作，bug 不再出现

### 回归测试

```bash
# 运行相关测试
sbt testOnly *SessionActorSpec
```

- [ ] 现有功能不受影响

---

## 关联

- Depends on `xxx.md` — 前置依赖
- Blocks `yyy.md` — 阻塞后续工作
- Related to `zzz.md` — 相关但无依赖
- Supersedes `www.md` — 替代旧 issue
- Introduced by `commit/PR` — 引入该问题的变更

---

## 变更文件

| File | Action | Notes |
|------|--------|-------|
| | | |

> 创建时填写"计划"，关闭前更新为"实际"。

| `Foo.scala` | **Modify** | 修复逻辑 |
| `FooSpec.scala` | **Modify** | 补充回归测试 |

---

## [结束] 关闭原因

> 见 `_glossary.md` → 关闭原因。必须选择一项。

- [ ] 已修复
- [ ] 重复 — 重复 issue：
- [ ] 无法复现
- [ ] 不予处理 — 理由：
- [ ] 已过时
- [ ] 已取消 — 理由：

---

## [结束] 复盘（可选）

> `[结束]` 时填写。若关闭原因为"重复"/"无法复现"/"已过时"，可跳过。

| 问题 | 回答 |
|------|------|
| 为什么这个 bug 会发生？ | |
| 为什么现在才发现？ | |
| 如何防止同类问题？ | |
| 工时预估偏差原因？ | |
