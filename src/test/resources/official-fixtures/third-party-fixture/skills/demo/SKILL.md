---
name: demo
description: Fixture skill body carried by the official-identity fixture packages.
---

# demo

Fixture skill: exists so the package passes the load rule "skills/ or mcp.json, at least one"
and never gets refused for that reason instead of for the reason under test.
